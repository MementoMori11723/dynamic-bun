{
  "name": "dynamic-bun",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "Build": "bun build index.ts --compile --minify --target=browser --outfile=server",
    "dev": "bun run --watch index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}